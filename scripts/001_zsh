#!/usr/bin/env bash

if ! type "zsh" > /dev/null; then
  . $PWD/lib/apt_install
  apt_install zsh autojump

  sudo chsh -s $(which zsh) $(whoami)
  curl -L git.io/antigen > ~/.config/antigen.zsh
fi

. $PWD/lib/link_file
link_dotfile $PWD .zshrc
link_dotfile $PWD .aliases